// Point these at your deployed backend before shipping. For local testing
// against `npm start` in haky-backend, the defaults below already work.
window.HAKY_CONFIG = {
  API_BASE_URL: 'http://localhost:8080',
  WS_BASE_URL: 'ws://localhost:8080',
};
